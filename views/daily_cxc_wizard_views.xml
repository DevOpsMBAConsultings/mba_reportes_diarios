<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ══════════════════════════════════════════════════════════════════
         Vista Form: Wizard Cobros de Cuentas por Cobrar (CxC)
         ══════════════════════════════════════════════════════════════════ -->
    <record id="view_daily_cxc_wizard_form" model="ir.ui.view">
        <field name="name">mba.daily.cxc.wizard.form</field>
        <field name="model">mba.daily.cxc.wizard</field>
        <field name="arch" type="xml">
            <form string="Cobros de Cuentas por Cobrar (CxC)">
                <group>
                    <group>
                        <field name="date_report"/>
                        <field name="company_id" groups="base.group_multi_company"/>
                    </group>
                </group>
                <footer>
                    <button name="action_print_report"
                            string="Generar Reporte PDF"
                            type="object"
                            class="btn-primary"
                            icon="fa-print"/>
                    <button string="Cancelar" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <!-- Acción de ventana del wizard (modal clásico) -->
    <record id="action_daily_cxc_wizard" model="ir.actions.act_window">
        <field name="name">Cobros de Cuentas por Cobrar (CxC)</field>
        <field name="res_model">mba.daily.cxc.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>

    <!-- Acción de Cliente Dinámico (OWL Frontend - Estilo Cybrosys) -->
    <record id="action_daily_cxc_client" model="ir.actions.client">
        <field name="name">Cobros de Cuentas por Cobrar (CxC)</field>
        <field name="tag">mba_daily_cxc_report</field>
    </record>

    <!-- Menú Directo: Contabilidad → Reportes → Cobros CxC (Dinámico) -->
    <menuitem id="menu_daily_cxc_dynamic"
              name="Cobros CxC (Dinámico)"
              parent="account.menu_finance_reports"
              action="action_daily_cxc_client"
              sequence="98"/>

</odoo>

